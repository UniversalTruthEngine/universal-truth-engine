# Machine-Readable Schema Notes

Each canonical truth should include a `machine-readable.json` file.

The global `truth-graph.json` connects entries into a navigable knowledge graph.

Future fields may include:

- spatial coordinates for visual map rendering
- dependency strength
- semantic similarity
- reconstruction difficulty
- confidence stability
- civilisational leverage
- ethical hazard level
- proof type
- contradiction links
- successor truths
