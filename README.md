# UTE Map Data Integrity v1

This package adds a map-data standard, truth index, baseline integrity report, and a tool for checking alignment between the Fact Vault and the interactive map.

The purpose is to keep the archive and map synchronised as UTE grows.
