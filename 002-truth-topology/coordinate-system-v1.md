# UTE Coordinate System v1

Stable coordinates are navigational addresses in the UTE knowledge cosmos.

Dynamic coordinates may be generated by layout engines and can change as the map grows.

A truth's identity is its Stable ID, not its position.
