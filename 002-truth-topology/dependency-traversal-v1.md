# Dependency Traversal v1

Selecting a node in the UTE map should be able to reveal its recursive dependency chain.

This allows users to inspect not only a truth, but the foundational path required to reconstruct it.

In the visual map:
- selected node remains bright
- dependency ancestors remain bright
- unrelated nodes dim
- active dependency edges brighten
